{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "server",
      "type": "node",
      "request": "launch",
      "runtimeArgs": [
        "--nolazy",
        "-r",
        "ts-node/register/transpile-only"
      ],
      "args": ["src/server/main.ts"],
      "cwd": "${workspaceRoot}"
    }
  ]
}
